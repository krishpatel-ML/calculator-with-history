# calculator-with-history
A simple Python calculator that performs basic arithmetic operations(+,-,*,/) and saves calculation history to a file.User can view,clear,or exit the program using commands 
